extends layout

block content
  link(rel='stylesheet', href='/stylesheets/cover.css')
  body(class="text-center")
    div(class="cover-container d-flex h-100 p-3 mx-auto flex-column")
      header(class="masthead mb-auto")
        div(class="inner")
          h3(class="masthead-brand") Vote4
          nav(class="nav nav-masthead justify-content-center")
            a(class="nav-link active" href="#") Home
            a(class="nav-link" href="/sign-up") Sign up
            a(class="nav-link" href="/login") Login
      main(role="main" class="inner cover")
        h1(class="cover-heading") Vote4
        p(class="lead") Create custom polls with live results
        p(class="lead")
          a(href="/sign-up" class="btn btn-lg btn-secondary") Sign up
      footer(class="mastfoot mt-auto")
        div(class="inner")
          p Vote4 by 
            a(href="https://github.com/domfarnham") Dom Farnham 
            a(href="https://twitter.com/jscodebook") @jscodebook.
